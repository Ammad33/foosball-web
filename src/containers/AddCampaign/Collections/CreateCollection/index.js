import { Grid } from '@material-ui/core';

const CreateCollection = () => {
    return (
        <Grid container>
            <Grid item xs={12}>

            </Grid>
        </Grid>
    );

};

export default CreateCollection;